# V2 status ledger

## Authority and scope

This is the shared handoff for the user's **V2 Prompt 1 (audit, bugs, cleanup and polish), Prompt 2 (new core features), and Prompt 3 (AI, automation and integrations)**. These prompt numbers are independent of the original Phase 0–8 roadmap and historical Experience Evolution P0–P11. The current implementation and the approved September 15 artwork design supersede older visual proposals.

Prompt 1 is **COMPLETE within the bounded audit described below** (2026-09-16). The approved execution plan was: establish this ledger; audit and repair security/ownership gaps; correct the reproduced shared navigation hover defect; verify existing UI before changing it; inspect cleanup and production readiness; run regression/build/browser checks; update this ledger and IMPLEMENTATION_STATUS. No public API or schema change was required. Deployment and unverified historical symptoms remain explicitly recorded, not silently marked solved.

Status vocabulary: COMPLETE, PARTIAL, BUG, MISSING, OUTDATED, REDUNDANT, DEFERRED, REMOVE CANDIDATE. A COMPLETE audit means the bounded checks recorded here passed, not certification or a claim about every possible device/attack. Historical symptoms without reproduction are explicitly distinguished from verified defects.

## Execution decisions

- Preserve the existing feature branch and eight dirty paths from the earlier one-command development change. Entry index/worktree diffs and artwork hashes are retained locally in ignored `artifacts/v2-prompt1/`. No commit, branch operation, or deployment is authorized.
- Ruling: work in the existing checkout rather than create a branch/worktree — AGENTS.md forbids branch operations without explicit permission, and the approved plan preserves this checkout's unfinished work. Changes stay reviewable in the working tree.
- Ruling: reuse this ledger instead of a second implementation-status ledger from a workflow skill — the user explicitly requires one shared V2 source of truth. Runtime evidence stays under ignored artifacts.
- Preserve top navigation and the closed overlay drawer. Plan is a navigation group opening Tasks, not a missing page. Do not restore the superseded permanent sidebar.
- Preserve exact Joint artwork, Inter, Phosphor Bold, original PNG bytes, natural scrolling, existing four languages, themes and density choices. Generic polish instructions do not override the approved design.
- Account creation is administrative. Missing public registration, external sign-in and production email recovery are deliberate boundaries, not newly discovered authentication regressions.

## Requirement audit

| Prompt areas | Entry status and what already exists | Prompt 1 changes / remaining verification |
| --- | --- | --- |
| 1, 3, 4, 25, 27: repository and architecture | COMPLETE: React JS/JSX/Vite/Router/Query/RHF/Zod and ASP.NET Core/EF/Npgsql modular monolith. Prior redraws and delivery history inspected; baseline work preserved. | Bounded endpoint/asset/production source audit complete; no P0 defect found. P1/P2 findings addressed below. |
| 2, 29: persistent V2 state | MISSING → COMPLETE: IMPLEMENTATION_STATUS records historical deliveries but no three-prompt handoff existed. | This ledger created before substantial implementation and linked from IMPLEMENTATION_STATUS and README. |
| 5, 6, 20, 21: shared UI, artwork, accessibility, responsiveness | COMPLETE bounded audit: shared tokens/primitives, original artwork, two themes, mobile navigation, native scroll and reduced motion. | Fresh main-route screenshots inspected; narrow onboarding card corrected. Both browser engines verify reflow, contrast, keyboard and enlarged text. Physical-device and screen-reader certification are outside this audit. |
| 7: Today | BUG → COMPLETE: hero/workspace and real task/habit/mission/focus behavior existed; a narrow header-resize defect and cramped first-task card were found. | Header observes actual height; mobile onboarding button moves below copy. Historical general text-scaling symptom remains distinct and not reproduced. |
| 8: active navigation | BUG → COMPLETE, P1: active bottom border and unconditional hover text underline created two indicators. | Shared CSS fixed; browser regression covers active/hover/focus across all groups/themes. |
| 9: Plan and Goals | COMPLETE existing Plan grouping, task workspace, Goals filters/progress/details and empty states. | Newer layout preserved. Real empty/busy data and responsive controls verified; corrected task-scale coverage now uses actual mobile width. |
| 10: Life Areas | COMPLETE: ten owner-specific areas, aggregate counts, edit dialog and filtered links. All ten areas use supplied images, including the subsequent University/Tesla.jpg follow-up. See the subsequent image integration below. | Counts/ownership tests and responsive cards verified. Existing explicit links work without hover; no decorative card animation required. |
| 11: sidebar/navigation surfaces | OUTDATED older permanent-sidebar requirement; current top navigation plus drawer intentionally replaces it. | Header transition fixed; drawer scrolling, focus, search handoff and Focus navigation verified. No duplicate navigation. |
| 12: Search | COMPLETE existing navigation search with keyboard selection, Escape and focus handling. | Real browser suites verify navigation/keyboard behavior; additional isolated UI-fixture screenshots inspect light desktop, dark mobile and empty results. No universal search added. |
| 13: dialogs/forms | COMPLETE shared native Dialog and form controls, confirmation, drawer and task side panel. | Focus, closing, scrolling, validation, both themes and language layouts verified. Screenshot helpers now finish entry animations; no unnecessary primitive rewrite. |
| 14: authentication | COMPLETE existing login/logout, Remember Me, absolute expiry, stamp revocation, CSRF, protected keys and local recovery. Public registration intentionally absent. | Source audit and backend auth suite pass; browser session-choice/restart coverage recorded below. Production recovery remains deferred deployment work. |
| 15: multi-user isolation | COMPLETE bounded audit: owner claims/filtering and extensive existing two-account tests; no cross-user vulnerability found. | Extended reward/focus rejection tests include victim-state readback and paused-session resume; receipts/replays with the same key stay independent across owners. |
| 16: hardcoded personal data | COMPLETE bounded audit: operational pages use APIs; shared defaults and fictional isolated fixtures are intentional. | No misplaced private user dataset found; no migration/removal needed. |
| 17: Git hygiene | COMPLETE entry inventory: six modified files and two staged additions from prior development scripts; no unexplained nonignored untracked files. | Entry staging preserved. Reviewed new V2 ledger added explicitly; evidence/secrets/builds remain ignored. No commit, push or branch operation. |
| 18, 24: code cleanup | COMPLETE bounded audit: shared components and feature modules already established; no proven dead production implementation found. | Historical studies identified as a conditional archive candidate, not deleted. Incorrect viewport capture test repaired. |
| 19: assets | COMPLETE bounded audit: eight original PNGs and local fonts/icons; active/study/test references reviewed. | Original PNG bytes preserved. Images range from about 245KB to 1.90MB; largest are Today and Rolex. Retention is explicit design policy, not an unnoticed optimization omission. |
| 22: production readiness | PARTIAL: multistage container, same-origin SPA/API, health endpoints and explicit migrations exist. Public deployment is not complete. | Host publish and container build/runtime checks pass. Clarified key/host requirements; hosting-specific work is explicitly deferred below. |
| 23, 28: tests and verification | COMPLETE: baseline expanded with targeted navigation/resize/ownership regressions and corrected responsive evidence. | Final results and failures/reruns recorded below; all 11 isolated persistence/auth browser regressions passed. |
| 26: future systems | DEFERRED by explicit user scope. | No Prompt 2/3 functionality in Prompt 1. |
| 30: final report | COMPLETE handoff in this ledger and implementation summary. | Already-complete items, actual fixes, UI, isolation, cleanup, Git, verification, deferrals and remaining boundaries are distinguished. |

## Data ownership

User-owned: tasks/completions, commitments/missions, habits/schedules/logs, goals/progress entries, Life Areas, settings, focus sessions, XP ledger, activities, rewards/claims and idempotent action receipts. Child records inherit ownership through their owning parent where appropriate. Shared reference data: default area definitions, rank/XP rules, icon/artwork mappings, translations and licensed password dictionary. System configuration: database connectivity, authentication limits, Data Protection and deployment settings. No frontend-only owner filtering substitutes for backend enforcement.

Source audit found no exposed cross-user endpoint or account-cache defect. Authentication routes, areas/settings, tasks/today, habits/goals, progression/activity/ledger, rewards and focus were traced through claim-derived ownership and referenced-resource validation. Productivity keys include user ID and path; area/settings/draft keys include user ID. Logout and confirmed session expiry clear the query cache and CSRF token.

Existing Phase 1/2 tests already cover protected routes, CSRF, login/lockout, owned CRUD and foreign relationship rejection. Prompt 1 extends `Phase3Tests.NewResourcesArePrivateAndCommandsRequireCsrfAndActionIdentity` with foreign reward GET/PATCH/DELETE, foreign Focus resume/stop, and independent receipts for the same ClientActionId under two owners. The targeted Phase 3 suite passed 8/8. No backend production code, authentication contract or schema was changed.

Database ownership is principally enforced in application queries/commands. Several foreign keys reference the resource ID rather than a composite owner/resource key. No application path permitting mismatched owners was found; composite constraints are a possible future defense-in-depth change, not a confirmed vulnerability or required migration for Prompt 1.

## Confirmed fixes

| Finding | Root cause and change | Verification |
| --- | --- | --- |
| P1 duplicate active navigation underline | Shared primary-navigation hover selector applied text decoration on top of the active border. Restrict it to inactive links; preserve the active border and focus outline. | New browser test failed with `underline` instead of `none` before the fix and passed afterward across all five groups and both themes. |
| P1 Today header uses the wrong artwork/surface state after viewport resize | IntersectionObserver rootMargin captured the original header height. Desktop is 72px and mobile is 64px; crossing the breakpoint left the old threshold in place. Observe header size and rebuild the intersection observer only when the actual height or hero changes; clean up all observers. | New browser test failed at a 68px hero boundary after resizing to mobile, then passed desktop → mobile → desktop after the fix. The broader historical text-scaling complaint is not attributed to this bounded defect without matching reproduction. |
| P2 stale overview and local-production instructions | README overview stopped at Phase 2; local image names still said phase1 and host-publish guidance omitted explicit durable key/allowed-host configuration. | Updated to actual delivered capabilities, neutral local image naming and the existing hosting requirements. No TLS/authentication behavior weakened. |
| P2 narrow Today onboarding copy | The first-task card shared one flex row between icon, text and button, reducing text to approximately one short word per line at 320px. On narrow screens, use the existing grid layout primitives to give the button its own row and leave the text room to read. | Confirmed in final Chromium and Firefox 320px screenshots and the desktop/mobile/enlarged-text browser checks. |
| P2 inaccurate mobile task-scale evidence | The 0/3/30/300-task test passed a width to a capture helper that accepts browserName, so both captures stayed at desktop width and were filed under numeric browser directories. Set the viewport explicitly and include width in the filename. | Final browser suites must exercise actual 390px and 1440px layouts. Screenshot helpers finish animations for stable visual evidence; interaction/motion behavior is unchanged. |

## Cleanup and production audit

- No safe unused production route, JS/JSX component, hook or stylesheet was identified. `/start` and the public sanitized connectivity endpoint remain intentional diagnostics. No speculative source deletion was performed.
- **REMOVE CANDIDATE (conditional archive decision):** `client/design-reference/` and its dedicated signature/atelier build/review/tests are isolated historical design resources with fictional data, licensed assets and editable Blender sources. They are excluded from the application build; preserve them together unless a later explicit archive decision is made. Their filenames alone are not deletion evidence.
- All seven active PNG mappings match the approved design. The subsequent approved image integration activates the eighth, `Tutto passo.png`, for Creative. Original PNG bytes are preserved; no replacement, conversion or extra image system was introduced. Fonts/icons retain local license/provenance files.
- Source scan found no private application dataset in production React modules. Shared default areas and progression rules are reference data; browser/unit fixtures and study content are fictional. No tracked `.env`, key material, generated build output or identified credential-bearing connection string was found. This is a bounded source/tracked-file review, not a complete historical secret-scanning certification.
- Multistage Docker builds serve the compiled SPA and `/api/v1` from one origin; reserved API/health routes precede SPA fallback. Runtime is non-root. Data Protection has protected environment-specific storage and requires a persistent external volume for container recreation. Startup does not apply migrations.
- Public deployment remains **PARTIAL / DEFERRED**: actual AllowedHosts, trusted proxy handling, HTTPS/HSTS, CSP policy, operational password recovery, migration operations, health orchestration, CI, offsite backup and restore exercises need the hosting-specific deployment phase. Do not guess trusted proxies or introduce a container health policy before that decision. `/health/live` is liveness; `/api/v1/system/status` checks database connectivity without private data.

## Do not recreate

Today hero/workspace; Tasks/Inbox and Plan navigation; Goals and Habits with persisted history; ten Life Areas and their existing illustrations; XP/level/rank/rewards/activity; persisted Focus; Settings preferences and localization; search/menu/dialog primitives; cookie authentication, Remember Me and local recovery; one-command development startup. New requirements must first be compared with these implementations.

## Deferred

**Prompt 2:** advanced task scheduling, internal calendar, additional Today planning methods, advanced command/global search, and other new core product work. Existing foundations remain intact.

**Prompt 3:** AI assistant/classification/briefings, automation/news, health evidence libraries, learning/polymath systems, Garmin/Lyfta and other integrations, external calendar synchronization, intervention/friction/recovery engines, experiments, decision journal, skill graph, simulation, Life API, advanced offline synchronization and major PWA architecture. Future prompts must settle exact feature boundaries before implementation.

**Dedicated deployment work:** hosting/TLS/proxy configuration, operational recovery, offsite backups/restore drills and public operation. Prompt 1 verifies the current local production shape, not a public deployment.

## Verification and remaining issues

Planning evidence: `artifacts/v2-prompt1-baseline-20260916/`. Implementation evidence: ignored `artifacts/v2-prompt1/`. All account/domain test data is fictional; application testing uses disposable databases.

| Check | Result and boundary |
| --- | --- |
| Frontend unit/component and launcher tests | `npm run test -- --run`: 47/47 in 20 frontend files plus 3/3 launcher tests. |
| Frontend lint / production build | `npm run lint` and `npm run build` pass; 260 modules, main JS approximately 257KB / 80KB gzip. |
| Backend | `node scripts/run-dotnet.mjs test --configuration Release --no-restore`: Release build succeeds; 55/55 pass, zero skipped. Includes isolated PostgreSQL migrations, auth, ownership and expanded receipt tests. |
| Existing end-to-end regression | **11/11 passed**: six foundation/product/UX cases, two authentication cases, two API-restart persistence cases and one remembered-login case after both API/browser restart. Real disposable PostgreSQL and fictional account; database and owned services cleaned up. |
| Built-app Chromium | Final suite **15/15 passed**, including actual 0/3/30/300-task layouts at 390/1440px, two added regressions, all routes/themes and no unexpected signed-in console/page/server failures. |
| Built-app Firefox | **14/15 passed initially; the sole failing all-route test passed unchanged on focused rerun (1/1)**. All 15 checks verified, not claimed as a single clean suite run. Initial trace retained: one local Inter WOFF2 download warning; same resource later returned 200 and WOFF fallback returned 200. Root cause of that transient failure is not established; console assertions were not suppressed. |
| Viewports and interactions | Main-route captures 390/1440px; Today and shell also 320/768/1280/1920px; both densities/themes, four languages, 200% text, reduced motion, forced colors, keyboard/dialog focus, missing artwork and native scrolling. |
| Contrast | 134 semantic pairs per theme/density: minimum normal-text 4.564 light / 5.286 dark; controls/focus 3.002 light / 3.538 dark. This is token-role verification, not complete artwork-pixel contrast certification. |
| Visual review | Actual empty/populated Today, Tasks/Inbox, Goals, Habits, Areas, Focus, Progress, Activity, Rewards, Settings, Login, drawer and confirmation screenshots inspected across representative desktop/mobile/theme samples. Search additionally captured with all API requests intercepted using fictional fixtures; it is UI evidence, not persistence/security evidence. |
| Production shape | Host publish succeeds; final Docker image builds. Two foundation browser tests pass against the production container and a real disposable PostgreSQL database. Verified client-route fallback, JSON API 404, liveness, non-root UID, mode-700 key directory and database connectivity after container restart. No public HTTPS authentication or deployment claim. |
| Development startup | Duplicate `npm run dev` explicitly fails on occupied 5173 and stops only its new API child. Original API/Vite listener PIDs remain unchanged. Earlier clean-start/Ctrl+C verification is historical evidence in IMPLEMENTATION_STATUS; this audit does not stop the user's existing services to repeat it. |
| Code review / detector | Independent ownership, cleanup and change reviews complete; victim-state test coverage feedback addressed and re-reviewed. Impeccable detector on changed UI files exits successfully with no findings. |

The initial targeted Debug test invocation could not overwrite the user's running API executable. Release tests passed without stopping that service. Fresh empty-database migrations emit an expected missing-migration-history probe before applying migrations successfully. Nonblocking local npm `min-release-age` and test terminal-color warnings remain; browser API logs contain expected antiforgery no-cache header messages.

Remaining boundaries: the broader historical Today text-scaling report is not independently reproduced; physical devices, full assistive-technology certification and public hosting/TLS are not verified. The intermittent Firefox font warning is recorded above for follow-up if it recurs. No outstanding confirmed P0/P1 application defect was found by this bounded audit.

Local verification scripts, screenshots, traces, logs, browser profiles, publish output and Docker audit image stay local/generated. The disposable container/database/key volume and browser-runner databases/services were removed. Original eight PNG hashes match entry. The entry index is preserved; only the reviewed V2 ledger is a new maintained project file, added by its explicit path. No unexplained nonignored untracked project files remain. No commit, push, merge or branch operation occurred.

## Life Areas image integration - 16 September 2026

- Locked mappings: Food & Cooking: cooking.jpg; Travel: Polo 1.jpg; Work & Career: Work.jpg; Personal: personal.jpg; Creative: Tutto passo.png (PNG explicitly approved instead of absent JPG); Home & Plants: toscana.jpg; Health & Fitness: Ronaldo.jpg. Home is present.
- Preserved: Finance/Money.png, Style/Rolex.png, University SVG, ten user-owned areas, responsive 3/2/1 cards, editing, counts and filtered links. Mapping remains in AreaPage.jsx by stable area key; cropping remains in AreaPage.module.css. No API, schema, ownership or seed changes.
- Other placements preserved: Backround.png / Today, Gods plan.png / Goals, Muhammed ali.png / Progress, Tiger.png / Rewards, Odessey.png / Focus.
- Intentionally unused: Micheal jackson.jpg, Polo 2.jpg, Porsche.jpg, no risk no story.jpg. Retain these supplied assets without forced placement. Preserve actual filenames and original bytes.
- Future sessions must preserve the locked assignments and existing mapping source; no Prompt 2 or 3 features were introduced.
- COMPLETE. Verification: frontend 47/47 plus launcher 3/3, lint and production build passed. Focused Chromium browser checks 3/3 passed against production preview and an isolated PostgreSQL database: all nine images decoded at 1440/1024/768/390/320px in both themes; renamed area retained artwork after reload; editing, links, keyboard, reduced motion, 200% text and missing-image behavior passed. No browser console errors in the image flow. Desktop, compact and mobile screenshots inspected; portrait/landscape focal positions adjusted without changing card layout. Evidence stays ignored under artifacts/area-images-final; disposable test database and services cleaned up. No backend code or schema changed.

## Life Areas visual follow-up - 16 September 2026

COMPLETE. University now uses the inspected Tesla.jpg in the existing stable-key artwork mapping. Media height increased from 10rem to 14rem (desktop/tablet) and from 9rem to 12rem (mobile below 640px). The 3/2/1 grid, card widths, spacing and text surfaces remain unchanged. Focal positions: University 50% 55%, Personal 50% 72%, Home 50% 30%, Fitness 50% 46%. Other image mappings and positions are preserved. This supersedes the earlier University SVG and media-height notes; the SVG fallback remains available for unmapped keys.

Verification: frontend 47/47 plus launcher 3/3, lint and production build passed. Focused Chromium checks passed 3/3 against production preview and isolated PostgreSQL: ten images loaded at 1920/1440/1280/1024/768/390/320px in both themes, no horizontal overflow or console errors in the image flow; editing/persistence, navigation, keyboard, reduced motion, 200% text and missing images remain covered. Screenshots inspected at large desktop, laptop, tablet and mobile; Tesla, Personal, Home and Fitness subjects are visible and remaining compositions preserved. Evidence remains ignored in artifacts/area-proportions; disposable database/services were cleaned up. Tesla.jpg added to Git tracking without renaming or modifying bytes. No API/schema changes, commit or push.

## Home and Personal image replacement - 16 September 2026

COMPLETE. The latest approved mappings supersede the previous locked choices: Home & Plants uses toscana.jpg with object-position 50% 45%; Personal uses personal.jpg with its existing 50% 72% position. The shared AreaPage mapping, 14rem/12rem media heights, cover scaling, grid and all other assignments remain unchanged. Home.jpg and self respect.jpg are retained but now unused, alongside the previously listed unused assets. No API or database changes.

Production frontend build and lint passed. The focused Chromium Life Area test passed 1/1 against production preview and isolated PostgreSQL, verifying all ten mappings, decoded images, no overflow at seven widths (320-1920px), both themes, editing and persistence. No console errors in the image flow. Desktop/light and mobile/dark screenshots visually inspected; house, vineyard, person and computer are visible. Evidence stays ignored in artifacts/area-home-personal; disposable services/database cleaned up. Both new images added to Git tracking; no commit or push.

## Life Areas interaction and controls - 16 September 2026

COMPLETE. Shared cards now use restrained 1% scaling, 2px lift, soft elevation and area-tone glow for precise-pointer hover and visible keyboard focus. Reduced motion removes transforms/transitions while keeping static feedback; an open editor disables the card effect. Cards remain semantic sections, with no extra click handler or tab stop.

Tasks/Goals/Habits are three equal secondary link controls with translated labels and live counts, zero preserved and unavailable counts shown as a dash. Arrows removed; shared theme/control tokens provide hover, focus and pressed feedback. Existing areaId routes and backend filtering are unchanged; Habits continues opening its filtered library.

As explicitly approved, media height is now 16rem desktop/tablet and 14rem mobile, revealing more of Personal and Fitness while preserving cover scaling, all source assets and positions. Source and position metadata are together in the existing AreaPage artwork map; prior per-area image-position CSS rules were removed. This supersedes earlier media-height and mapping-shape notes.

Verification: frontend 47/47 plus launcher 3/3, lint and production build passed. Four focused Chromium checks passed, plus one additional language/touch check (5 passed across two runs). All ten cards hovered in both themes without neighbor layout shifts; keyboard focus, pressed feedback, reduced motion and dialog suppression passed. Real isolated PostgreSQL fixtures proved Tasks/Fitness, Goals/Travel and Habits/Personal filtering, exclusion of other-area content and selected-filter persistence after reload. All ten images decoded at 1920/1440/1280/1024/768/390/320px in both themes. Desktop, tablet and mobile screenshots inspected, including improved Personal/Fitness crops. Four languages at 320px with 200% text and an emulated touch device passed without overflow. No console errors in tested card flows. Missing artwork remains covered.

Evidence stays ignored in artifacts/area-interaction and artifacts/area-interaction-language; disposable database/services cleaned up. No new project files, assets, API/schema changes or unrelated page redesign. Existing staging preserved; no commit or push.

## Personal full-width desktop card - 17 September 2026

COMPLETE. Personal spans all three grid columns from 1200px, with equal image/content halves and a 20rem minimum height. The image fills the left half with existing cover/position metadata; content can grow and reserves room for the upper-right editor, with controls at the bottom. Below 1200px the original stacked card remains. This is a scoped CSS change; all image assignments, other cards, user sorting, routes, counts and shared interactions are preserved.

Verification: production frontend build, lint and diff whitespace check passed. Existing focused Chromium suite passed 5/5 with isolated PostgreSQL: seven widths 320-1920px, both themes, image decoding, editing, filtered navigation and reload, hover without neighbor shifts, keyboard, reduced motion, missing images, four languages with 200% text and touch. Desktop/light, laptop/dark and mobile screenshots inspected; the Personal row spans the desktop grid and mobile stays stacked. Evidence remains ignored in artifacts/area-personal-wide, including artwork/chromium/areas-images-Light-1440.png. Disposable database/services cleaned up. No new project files, backend changes, commit or push; existing staging preserved.

## Life Areas layout editor - 17 September 2026

COMPLETE. Customize layout (Tilpass oppsett) opens a local draft with drag handles, a compact overlay, drop markers, translated move-earlier/later controls and Save layout/Cancel. Keyboard and touch users can use the same move buttons; touch dragging uses a short hold. Entry/exit focus, movement announcements, reduced motion, pending-save disabling and retryable errors are handled. Existing navigation and metadata editing return on exit.

The existing default order is preserved, with Personal at the bottom until moved. Ordinary cards reorder freely within the shared grid. Personal can occupy any complete desktop row boundary (before 0, 3, 6 or 9 ordinary cards), including the top; moving ordinary cards preserves that boundary. Its wide presentation now comes from shared areaPresentation.js metadata, alongside the unchanged images and crop positions, instead of a Personal-specific CSS selector. Tablet/mobile retain the same saved sequence and responsive dimensions. Legacy non-boundary positions are normalized only in the draft and persisted only on Save.

PUT /api/v1/areas/order validates the exact complete owned set (including inactive areas), rejects duplicate/missing/foreign/unknown IDs and atomically updates existing SortOrder values. Authentication and CSRF are enforced. Names, activation and other users remain unchanged. No migration. The raw sort-order field was removed from the metadata dialog; optional PATCH compatibility remains. No browser storage of user layout and no duplicate configuration system. @dnd-kit/react is pinned at 0.5.0.

Verification: frontend production build and lint passed; frontend 50/50 and launcher 3/3 passed, with 5 focused layout/translation tests passing again after final copy changes. Backend Release build passed without warnings and PostgreSQL-backed suite passed 56/56, including new order/ownership/authentication/CSRF/invalid-set checks. Six relevant Chromium scenarios passed across the final suite and touch rerun: draft/cancel/failure/retry/reload, every wide row via keyboard, pointer reorder/Escape, touch drag/buttons with dark theme and reduced motion, all image mappings across 320-1920px/both themes, existing editing and filtered navigation, four-language enlarged-text/touch controls. The initial touch test incorrectly moved before the library's hold threshold; it now waits for drag activation and passes. Desktop/light and mobile/light/dark screenshots inspected. No page errors in the tested layout flow; runtime logs contain no application errors (existing split-server static-root and antiforgery cache warnings remain). Physical-device/screen-reader certification was not performed.

Evidence stays ignored under artifacts/area-layout*, with temporary databases/services cleaned up. Five shared source/test files added to tracking; prior work and staging preserved. No commit or push. Do not rebuild this as a second layout system or restore positional hardcoding.

## Life Areas filters and whole-card dragging - 17 September 2026

COMPLETE implementation; account-specific live save confirmation remains pending. Filter opens a responsive panel for My layout, localized alphabetical order, most/fewest open tasks, active goals and active habits, plus all/active/inactive and with/without content. Choices use URL parameters, combine without writes, preserve saved order for ties, and can be reset. Unknown counts are not zero; count-dependent controls/views wait for complete counts. Personal is a standard card under automatic sorting and wide in My layout. Customize layout clears viewing filters and edits the complete owned collection.

Drag can start on the image, heading or non-interactive card surface. Native image dragging is disabled in editing; interactive move buttons remain separate, keyboard controls and focus remain available. Touch uses hold-to-drag while quick swipes continue scrolling. Save 404 now explains that the running API lacks layout saving, preserving the draft.

The user's running Debug API predated the order endpoint. Replaced the verified project watcher/API processes with the current Debug API; normal Vite on 5173 now forwards PUT /api/v1/areas/order to an authenticated route (anonymous request returns 401). dev:api now uses watch --no-hot-reload so backend changes restart startup endpoint registration rather than leave a stale route table. Vite HMR remains unchanged. No backend contract/schema/authentication changes. A live authenticated check was attempted using local provisioning credentials, but login returned 401; no password reset, auth bypass or account-data mutation was performed. User was asked to retry Save in their existing session; do not claim that account-specific confirmation was obtained without a response.

Verification: production frontend build and lint passed, frontend 52/52 plus launcher 3/3 passed; Release backend build passed with no warnings and relevant PostgreSQL-backed RedesignTests 4/4 passed. Seven relevant Chromium scenarios passed across the suite and focused rerun: filters/count ordering/missing counts/URL reload, save 503 and 404 recovery/readback, image/text dragging and Escape, touch hold and swipe scrolling, existing all-image/responsive/edit flows, filtered Tasks/Goals/Habits navigation and four-language enlarged text. The first cold login exceeded the old test helper's 5-second navigation wait during concurrent builds; trace showed the login request still pending, so the helper now allows 15 seconds and the focused rerun passed. Desktop/light and mobile/dark filter screenshots were inspected. Evidence is ignored under artifacts/area-filters*. Isolated databases and services were cleaned up; the updated normal development API remains running.

Three new filter UI/logic/test files are tracked. Existing images, layout proportions and prior work are preserved. No commit or push.
